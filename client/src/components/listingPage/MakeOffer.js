import React from 'react';

function MakeOffer(){
    return (
        <section className="makeOffer">
            <button>Make an offer!</button>
        </section>
    )
}

export default MakeOffer;